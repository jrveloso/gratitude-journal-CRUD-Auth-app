<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gratitude Journal</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <main>
        <% if (locals.messages.errors) { %>
            <% messages.errors.forEach( el => { %>
                <div class="alert alert-danger"><%= el.msg %></div>
            <% }) %>    
        <% } %>
        <form id="create" action="/signup" method="POST">
            <input class="login-signup-input" type="text" name="userName" placeholder="User Name">
            <input class="login-signup-input" type="email" name="email" placeholder="Email">
            <input class="login-signup-input" type="password" name="password" placeholder="Password">
            <input class="login-signup-input" type="password" name="confirmPassword" placeholder="Confirm Password">
            <input class="login-signup" type="submit">
        </form>
    </main>
</body>
</html>